import React from 'react'

const App = () => (
    <div>
        <h1>Ufp Example 002 - React </h1>
        <p>This is a react component rendered using ufp-react to wrap and initialise it</p>
        <p>In this case a pure render function is used which is desirable to be main react component type </p>
        <div className="spinner" />
    </div>
)

export default App
